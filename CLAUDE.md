# ai-daily-web

`btc-daily-web` 에서 갈라져 나온 AI 카드뉴스 발행기다. 파이프라인 구조(수집 → 후보
선별 → 카드 10장 → 발행)는 같고, 도메인 용어집·브랜드·클러스터링이 다르다.

## 이 프로젝트만의 것 (btc-daily-web 과 다른 점)

| | btc-daily-web | ai-daily-web |
|---|---|---|
| 포트 | backend 8002 · frontend 5175 | **backend 8003 · frontend 5176** |
| 소스 | `asset=btc` · 영상 `topic=비트코인` | `asset=ai` · 영상 `topic=AI` |
| 등급 | btc / macro / other | **ai / industry / other** |
| 배제 축 | 알트코인 | **크립토** |
| 표지 인용구 | 오스트리아학파 경제학자 | **AI·컴퓨팅 인물** (`ai_quotes.json`) |
| 매체 간 중복 | 없음 (매체 11곳이라 불필요) | **`cluster_events` 필수** (매체 54곳) |
| 배포 | daily.onebitebitcoin.com | **daily.onebitecoder.com/ai** (서브패스) |

두 저장소는 완전히 독립이다. 한쪽 수정이 다른 쪽에 자동으로 반영되지 않는다.

## 용어 규칙 (MANDATORY)

**AI 고유명사는 `backend/app/wording.py` 의 `BANNED_TERMS` 를 따른다.** 발행 직전
게이트가 강제하므로 어기면 `push_edition.py` 가 POST 전에 막는다.

- 앤스로픽 → **앤트로픽** · 할라피뇨 → **할라페뇨** · 제미니 → **제미나이**
- 데이터 센터 → **데이터센터** · 챗지피티 → **챗GPT** · 오픈에이아이 → **오픈AI**

이 목록은 지어낸 게 아니라 하루치 소스 500건에서 실제로 갈린 표기다. 새 모델·회사
이름이 두 갈래로 들어오는 걸 보면 그 표에 한 줄 추가하고 그날 발행분에 적용한다.

`btc-daily-web` 의 "BTC 금지" 규칙은 여기 없다. 대신 위 표기 고정이 그 자리를 맡는다.

## 도메인 경계 (MANDATORY)

**이 카드뉴스는 AI 온리다.** 코인·크립토 소재는 카드에 싣지 않는다.

`asset=ai` 피드에는 토큰포스트·블록미디어발 기사가 상당수 섞여 들어온다(하루 500건
중 128건). `CRYPTO_TERMS` 가 이걸 `other` 등급으로 밀어내지만 완벽하지 않으므로,
카드 10장을 고를 때 한 번 더 거른다. 다만 **AI 인프라에 돈이 어떻게 흐르는지**를
다루는 기사는 코인 매체가 썼더라도 AI 소재다 — 매체가 아니라 내용으로 가른다.

## 후보를 읽을 때 (MANDATORY)

후보 항목에 `cluster_size` · `also_covered_by` · `cluster_titles` 가 붙어 있다.

- `cluster_size` 가 크면 그날 여러 매체가 동시에 다룬 사건이다 — 화제성 신호다.
- **`cluster_titles` 를 반드시 훑어라.** 클러스터링이 주제가 인접한 다른 사건을 가끔
  같이 묶고(실측: 스페이스X 베라 CPU 도입 + 엔비디아 베라 루빈 성능 확장), 반대로
  표기가 갈리면 같은 사건을 쪼갠다. 대표 제목만 보고 카드를 쓰면 다른 사건의
  매체 수를 빌려 쓰게 된다.
- 대표는 "군에서 가장 앞선 것 중 이미지가 있는 것"이다. 이미지와 `link` 는 항상 같은
  기사에서 나온다 — **다른 매체 이미지를 가져다 붙이지 마라.**

## reference/ 디렉토리

원본 카드뉴스 템플릿(HTML)과 그 샘플 콘텐츠다. btc-daily-web 시절 그대로 두었다 —
디자인 레퍼런스이자 스키마 테스트가 "유효한 페이로드" 표본으로 쓰는 파일이라,
브랜드 문자열이 `BTC DAILY` 인 것은 의도된 것이다. 발행물과는 무관하다.
