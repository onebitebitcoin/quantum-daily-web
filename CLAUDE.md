# quantum-daily-web

`ai-daily-web` 에서 갈라져 나온 양자 카드뉴스 발행기다. 파이프라인 구조(수집 →
후보 선별 → 카드 10장 → 발행)는 같고, **수집 주기와 발행 주기가 다르다.**

## 이 프로젝트만의 것 (ai-daily-web 과 다른 점)

| | ai-daily-web | quantum-daily-web |
|---|---|---|
| 포트 | backend 8003 · frontend 5176 | **backend 8004 · frontend 5177** |
| 컨테이너 포트 | 8021 | **8022** |
| 소스 | `asset=ai` · 영상 `topic=AI` | **`asset=quantum` · 영상 `topic=양자컴퓨팅`** |
| 수집 창 | 뉴스 36h · 영상 48h | **뉴스 168h · 영상 168h (7일)** |
| 발행 주기 | 매일 06:10 | **주 1회** (수집은 매일 돌아간다) |
| 등급 | ai / industry / other | **quantum / physics / other** |
| 배제 축 | 크립토 | **양자화(quantization) · 퀀텀닷 TV · 퀀텀점프(비유)** |
| 표지 인용구 | AI·컴퓨팅 인물 | **물리·양자 인물** (`quantum_quotes.json`) |
| 배포 | daily.onebitecoder.com/ai | **daily.onebitecoder.com/quantum** |

세 저장소(btc·ai·quantum)는 완전히 독립이다. 한쪽 수정이 다른 쪽에 자동으로
반영되지 않는다.

## 수집은 매일, 발행은 주 1회 (MANDATORY)

하루 카드 후보가 14~17건이라 매일 10장을 뽑으면 선택비가 1.5:1 밖에 안 된다.
한 주를 모으면 후보가 24~129건이라 10장을 고를 여유가 생긴다(2026-09-20 실측).

- **수집**: `my-news` 크롤러가 1시간 주기로 돈다. 이 저장소는 따로 수집하지 않는다.
- **발행**: `scripts/weekly-cron.sh` 가 주 1회 창 168시간으로 카드를 만든다.

창을 168시간으로 늘릴 때 `NEWS_BUCKETS` 를 7로 같이 올려야 한다. 4로 두면 한
버킷이 42시간이라 특정 요일 기사가 몰려 들어온다.

## 도메인 경계 (MANDATORY)

**범위는 양자컴퓨팅에 한정하지 않는다.** 양자역학·양자광학·양자센싱·양자통신 등
양자물리 전반을 담는다. 컴퓨팅만 받으면 한국어 기사가 하루 3.7건에 그친다.

대신 아래 셋은 카드에 싣지 않는다. 셋 다 실측으로 후보에 섞여 들어온 것이다.

- **양자화(quantization)** — LLM 경량화 기사다. `4비트 양자화`, `Vector Quantization`.
- **퀀텀닷(QD) TV** — 디스플레이 기사다. `삼성, 올해 QD TV 생산 80% 줄인다`.
- **퀀텀점프** — 비유다. `K-방산, 자주포 넘어 퀀텀점프`.

크립토 각도는 내용으로 가른다. `비트코인이 양자컴퓨터에 뚫린다` 류 시황 기사는
빼되, 양자내성암호(PQC) 전환 자체를 다루는 기사는 양자 소재다.

## 후보를 읽을 때 (MANDATORY)

`ai-daily-web` 과 같다. `cluster_size` · `also_covered_by` · `cluster_titles` 가
붙어 있고, **`cluster_titles` 를 반드시 훑어야 한다.** 다만 양자 코퍼스는 매체가
적어 같은 사건을 여러 곳이 쓰는 일이 드물다 — 2026-09-18 실측에서 기사 95건이
사건 93개로 갈렸다. 클러스터가 크면 그만큼 진짜 화제다.

대표는 "군에서 가장 앞선 것 중 이미지가 있는 것"이다. 이미지와 `link` 는 항상 같은
기사에서 나온다 — **다른 매체 이미지를 가져다 붙이지 마라.**

## 번역

카드 절반 이상이 영문 매체에서 온다(The Quantum Insider·Quantum Computing
Report·Phys.org). `ai-daily-web` 이 이미 해외 출처 36% 로 돌고 있으니 새로 만들
단계는 없다 — 한국어 제목 + 영문 부제 형식을 그대로 따른다.

## reference/ 디렉토리

원본 카드뉴스 템플릿(HTML)과 샘플 콘텐츠다. 브랜드 문자열이 `BTC DAILY` 인 것은
btc-daily-web 시절 그대로 둔 것이고 의도된 것이다. 발행물과는 무관하다.
